name: Build SimplerHidWrite

on:
  push:
    branches: [ master ] # 你的默认分支是 master

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # 1. 设置 .NET 环境 (SimplerHidWrite 是老项目，通常兼容 .NET Framework 或旧版 Core)
    # 这里我们尝试使用较新的 SDK，它通常能向后兼容构建旧项目
    - name: Setup .NE
